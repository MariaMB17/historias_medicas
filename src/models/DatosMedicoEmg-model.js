export default class DatosMedicosEmg {
  constructor (id, personaId, turno, fecha) {
    this.id = id
    //eslint-disable-line
    this.persona_id = personaId
    this.turno = turno
    this.fecha = fecha
  }
}
