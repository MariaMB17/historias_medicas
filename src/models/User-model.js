export default class User {
  constructor (name, email, password, rememberToken) {
    this.name = name
    this.email = email
    this.password = password
    this.rememberToken = rememberToken
  }
}
